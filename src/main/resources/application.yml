server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/usermanager
    username: root
    password: 1234
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

jwt:
  secret: secret